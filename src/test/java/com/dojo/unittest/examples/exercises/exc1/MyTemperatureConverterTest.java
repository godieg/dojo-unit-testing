package com.dojo.unittest.examples.exercises.exc1;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;
import static org.junit.jupiter.api.Assertions.assertEquals;

class MyTemperatureConverterTest {

    @Test
    void shouldCallDummyTest() {
        assertEquals(1 + 1, 2);
        assertThat(1 + 1).isEqualTo(2);
    }

    /**
     * Construir el conjunto de pruebas unitarias suficientes para la clase MyTemperatureConverter
     * Implementar la misma prueba utilizando JUnit5 y assertJ
     */

}
